// AgentsKit holds everything that decides anything: the protocol, the record, the
// daemon and the client the app talks to it with. The app target and the helper are
// shells around this, so all of it is reachable from `swift test`.
