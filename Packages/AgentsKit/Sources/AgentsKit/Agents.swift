/// A placeholder so the skeleton builds, runs and is tested end to end before the
/// first feature is specified. The first feature deletes it.
public enum Agents {
    public static let greeting = "Agents"
}
