# The till

A small shop's till, for the colour samples (041). It takes **money** and _remembers_ it.

## Using it

1. Open it with `openTill()`.
2. Sell with [the sell call](https://example.com/sell).

> Mind the step.

```swift
let till = Till()
```

- one
- two
